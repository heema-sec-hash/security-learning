\# Day 6 Complete – 29.06.26



\## Review Summary



| Command | I Used It For |

|---------|---------------|

| `base64 -d` | Decoding Base64 data |

| `tr` | Decoding ROT13 |

| `xxd -r` | Hexdump to binary |

| `gzip -d` / `bzip2 -d` | Decompressing files |

| `tar -xf` | Extracting tar archives |

| `ssh -i` | Using SSH key to connect |

| `chmod 600` | Securing private key |

| `nc` | Sending data over network |

| `find` | Locating files by size/user/group |

| `grep` | Searching inside files |

| `strings` | Extracting readable text from binaries |



\## Real-World Connections

* Base64 — used in API tokens, malware, and email attachments
* ROT13 — simple obfuscation used in CTFs and forums
* gzip/bzip2 — compression used in log files, backups, and data transfer
* chmod 600 — securing SSH private keys so only I can read them
* find and grep — used to locate files and search for sensitive data in logs or code"
* Base64 is also used in JWTs, API keys, malware encoding
* SSH keys are used on cloud servers (AWS, Azure, GCP)
* Netcat is used for port testing, reverse shells, file transfer
* find/grep are also used in log analysis and incident response
* Compression tools are also used for backups, logs, and malware packing



\## Notes / Issues

\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_



\## Next: Week 2 — HTTP/curl + Income Resumes

